service: streamlit-app-prod 1 #< change this to something else

instance_class: B2

env_variables:
  STREAMLIT_SERVER_PORT: 8502

entrypoint: streamlit run streamlit_app.py --server.port 8502 #< change 'app.py' to whatever entry point you have
